#include <bits/stdc++.h>

using namespace std;

int singleNumber(vector<int> &nums)
{
  for (int i = 0; i < nums.size(); i++)
  {
  }
}

int main()
{
  vector<int> a;
  int t;
  while((cin >> t))
  {
    a.push_back(t);
  }
  for(t = 0;t<a.size();t++) cout << a[t];

}