#include <bits/stdc++.h>

using namespace std;

int main() {
    freopen("in.caitui", "w", stdout);
    int n = 5;
    srand(time(NULL));
    while (n--) {
        int m = 3;
        int max = 32147, min = -32147;
        for (int i = 0; i < m; i++) {
            int rand = 5 + (rand() % (20 - 5 + 1));
            if (rand > min)
        }
    }
}
